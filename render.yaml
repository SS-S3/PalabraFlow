services:
  - type: web
    name: palabraflow
    env: python
    region: oregon
    plan: free
    rootDir: server
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
      - key: PORT
        value: 10000
